extends layout

block content

  br
  .row
    .col.s12.m4.offset-m4
      .card.blue-grey.darken-1
        .card-content.white-text
          span.card-title(tabindex="0") #{user.nombre} #{user.apellidos}
          p(tabindex="0") El email con el que se ha registrado es: #{user.email}
